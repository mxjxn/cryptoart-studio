import { directCastConversationDefaultQueryOptions } from '../directCastConversationDefaultQueryOptions';

/**
 * Default query options for direct cast conversation historical messages.
 * Re-exports the shared configuration to maintain consistent naming.
 */
const directCastConversationHistoricalMessagesDefaultQueryOptions =
  directCastConversationDefaultQueryOptions;

export { directCastConversationHistoricalMessagesDefaultQueryOptions };
