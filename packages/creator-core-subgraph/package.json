{
  "name": "creator-core-subgraph",
  "version": "0.1.0",
  "description": "Subgraph for indexing Manifold creator-core events (extensions, mints, transfers, royalties)",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ creator-core",
    "create-local": "graph create --node http://localhost:8020 creator-core",
    "remove-local": "graph remove --node http://localhost:8020 creator-core",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 creator-core",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.0",
    "@graphprotocol/graph-ts": "0.37.0"
  }
}
